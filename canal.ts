export class Canal {
  nombre: string
  baner: string
  nombreStreamer: string
  descripcion: string

  constructor(nombre: string, baner: string, nombreStreamer: string, descripcion: string) {
    this.nombre = nombre
    this.baner = baner
    this.nombreStreamer = nombreStreamer
    this.descripcion = descripcion
  }
}